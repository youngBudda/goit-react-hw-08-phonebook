import styled from 'styled-components';

export const StyledContacts = styled.ul`
  list-style: none;
  padding: 0;
  margin: 0;

  div.animated {
    width: auto;
    height: auto;
    border: none;
    box-shadow: none;
  }
`;
